- @meta_title =  "#{d(event.start, "%d.%m.")} #{event.name}"
- @meta_description =  "#{d(event.start, "%d. %B %Y")}: #{event.description}"
- @meta_keywords = event.name.split + %w{ Events Veranstaltungen Kalender Veranstaltungskalender Feier Party Fest }

%h1= "#{event.name} am #{d(event.start, "%d.%m.")}"

= haml(:_single_event, :layout => false, :locals => { :event => event, :standalone => true })

- if event.attending.any?
  %h2 Die Leut hier ham schon zugesagt
  %ul#attendees.clearfix
    - event.attending.each do |attendee|
      %li
        %a{ :href => "https://www.facebook.com/profile.php?id=#{attendee.id}", :title => attendee.short_name }
          %img{ :src => "http://graph.facebook.com/#{attendee.id}/picture?type=square" }

%p.facebook
  Tipp: Du findest alle Hergershäuser Veranstaltungen auch auf facebook:
  %a{ :href => "https://www.facebook.com/hergershausen?sk=events" }
    facebook.com/hergershausen